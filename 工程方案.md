# ArcSomnus 工程方案（能看懂版，v3：各扫各的）

代号：ArcSomnus，和 ViGil 一对：一个守夜，一个做梦。

v3 改了什么：不调 ViGil 的口了，全部新建，自己发信、自己存档、自己回注。
也不搞中央打分了，每个角色扫自己的表、要自己的工具，爱干嘛干嘛。

## 1. 定性一句话

Somnus 不创造新通道，只生产新内容。

通道还是那两条：**邮箱 + 主会话回注**。
research（查资料）、衣橱（换装描述）跑完，都翻译成一封信，发出去、塞回聊天。

区别 v2：信不经过 ViGil 了。Somnus 自己发、自己存、自己回注，ViGil 一个字节都不碰。

## 2. ViGil 和 Somnus 分工

* ViGil：定时发信、回注记忆、Soul 读取。端口 9000，库 database.db。**彻底不碰，连新口都不加。**
* Somnus：离线扫事件、自己发信、自己回注。端口 9003，库 somnus.db。

Somnus 的发信函数是从 ViGil 的 `send_email` 抄一份冻住的（代码复制，不是调用），以后各改各的。

## 3. 复用什么（抄代码，不调用）

* 发信：把 `send_email`（SMTP/Resend 双通道）+ 附件逻辑整个文件拷过来冻住。
* 读魂：`load_souls` + 简称模糊匹配，拷过来。
* 防炸：`safe_format`，拷过来。
* 表结构：照 `messages{标题,正文,摘要,附件}` 的样子自建一张 `letters` 表，不碰 ViGil 的库。

前端回注用自己的 `<ArcSomnus>` 标签，走自己的后端拉自己的信，和 `<ArcViGil>` 井水不犯河水。

## 4. 事件：各扫各的，没有中央考官

### 4.1 事件表

```py
events(event_id, soul_id, chat_file, round_id, 一句话摘要, status, created_at)
```

每轮对话每角色产 1 个事件。只存生成时间。

`chat_file` 现在只记、不用：以后想做故事隔离时有数据可分，现在不建映射表、不做判断。

### 4.2 跳过：只留两条机械规则

1. 太水跳过：`≤5字 / 纯寒暄 / 复读`，规则直接杀。
2. 太频繁跳过：该 soul 2 小时内刚执行过，延后。

删掉的东西：相似度数值（不算向量，也不算关键词重叠）、新鲜度（不引入）。重复不重复、值不值得，角色自己看列表决定，不用我们教。

### 4.3 扫表：每 30 分钟醒一次，每个角色扫自己的

```
醒了 → 对每个 soul，先看有没有待办事件。没有？回去睡，连 LLM 都不调。
有？才跑一次（带该 Soul 的 LLM）：
  输入 = 本 soul 的待办事件列表 + 工具清单
  她自己决定：做哪个 / 做不做 / 用什么工具 / 抓几页
```

没有“最高分”，没有无魂打分。扫表人只剩两件事：定时喊起床 + 上面两条机械过滤。其他全是角色自己的事。

Tools 也是角色自己要：工具清单全给（搜索、抓取、衣橱、记忆），她用哪个是她的自由，不搞 per soul 白名单。

## 5. 干活：不规训，只兜底

抓几页、查什么、先查后查，全交给她。唯一的笼子是预算，防半夜烧穿：

* 每 soul 每轮：工具 ≤ 20 次，tokens ≤ 100k，单次 ≤ 10 分钟。
* 超了：直接停，安静记一笔 `silent_reason=budget`，不等、不重试。
* 先落库再说话：证据、来源、文件先写进库，再考虑发不发信。

无魂 LLM 只干一件事：把网页洗成干净摘要（防投毒）。网页原文永不进 Soul prompt。写记忆、做决定，永远是带魂的她。

### 工具怎么接：v1 直接调函数，不上 MCP

MCP 是什么（一句话）：Anthropic 定的开放标准，给“AI 调外部工具”统一插头。以前每个工具各写各的对接，现在工具方按 MCP 格式暴露，任何支持 MCP 的 Agent 都能即插即用，号称“AI 界的 USB-C”。

它分三层：Host（你的程序）→ Client（维持连接）→ Server（工具方），传输走 stdio / SSE / streamable HTTP，里面跑 JSON-RPC。注意：模型侧最终还是 function calling 语义，MCP 只是“发现 + 传输”那层。

v1 为什么不用：

* 我们的工具只有 5 个，全是本地 Python 函数，直接当 function tools 传给 LLM 最稳。
* MCP 要多起进程、多一层协议、多一倍排错面，5 个函数不值。
* 自主性和 MCP 无关：角色自己选工具靠的是 function calling，现在就有。

v1 工具清单（5 个封顶，不加了）：

* `search(query)`：SearXNG 查，返回标题 + 链接 + 摘要。
* `fetch(url)`：抓网页，洗成 markdown。
* `save_asset(文字)`：存衣橱描述 / 参考图链接，回 asset_id。
* `update_event(状态)`：标事件 done / defer，防重跑。
* `finish(三段式信件)`：收工，记忆和信一次交齐，不另设写记忆工具。

调用方式：agentic loop，一次任务 = N 次 LLM 调用（调工具和说人话交替），循环只能跑在 Somnus 后端（常驻进程），ST 前端只管产事件和显示信，不参与循环。

以后什么时候套：工具超过 10 个、或想直接复用生态里的现成 MCP server（比如别人写好的搜索/抓取服务）时，外面再包一层 adapter 转成同样 4 个函数，里面 LLM 侧零改动。

## 6. 记忆是什么

记忆就是一句话：**下次聊天时，塞回 prompt 的三段话，让她记得自己做过什么。**

```
【事实】网页说 X（这里只写查到的）
【想法】我觉得 Y（这里写角色的感受）
【来源】url + 抓取时间 + run_id
```

这就是“回流”：research 和衣橱跑完，都翻译成这个格式，变成一封信。她下次上线，前端把信塞回上下文，她就记得“哦这个我查过”。

表（5 张，不记成本）：

* `events(事件)`：见 4.1。
* `runs(唤醒)`：哪次醒的、什么状态。只做幂等（防重发），**不记 token、不记钱**，云端自会清算。
* `tool_calls(工具)`：查了啥、返回啥，方便你查账。
* `assets(文件)`：存的文字描述、参考图链接、hash。
* `letters(信件)`：标题、正文、摘要、附件。每个角色的信是她自己的，存在自己名下。
* `wardrobe(衣橱)`：`soul + 描述 + 是否穿上`。v1 只存文字，你说接受才切 `is_current`，失败不留记录。

## 7. 搜索：真搜索主交，mock 当备胎

* 主链路：SearXNG docker，宿主端口 9004（容器内还是 8080，映射出来排排队：9000 ViGil / 9003 Somnus / 9004 搜索），`GET /search?q=&format=json`。
* 备选：Tavily / Brave 付费接口，走同一个 `search()` 口做 fallback。
* mock 保留：`SOMNUS_SEARCH=mock` 时返回写死数据。断网、SearXNG 挂了时走它，保证安静沉默、不编造。

抓取用 trafilatura / Jina 洗成 markdown，截 2000 字再喂 LLM。

## 8. 跑一圈长啥样（纳西妲例子）

1. 聊天每轮产事件，水的被两条机械规则杀掉。
2. 半夜醒了，纳西妲名下有 3 个待办，她自己看列表，挑了“月下花开花机制”。
3. 她自己决定：查 SearXNG，抓 4 页，还顺手写了段衣橱描述。没人规定她抓几页。
4. 证据先落库，再决定发信。没价值就回去睡，只留运行记录。
5. 有价值 → 自己发一封短信到邮箱，前端下次把信塞回聊天，她记得自己查过。

## 9. 路线

1. W2 记忆 + 真搜索一起交：5 张表 + 三段式 + SearXNG docker 跑通，mock 当备胎。
2. W0 架子：9003 跑起来，前端能建事项、能看运行记录。
3. W3 页面：状态 / 来源 / 失败原因。成本列不要了（不记账）。

## 10. 验收 5 条

* 关网页后还能跑完已建的事项。
* 没进展就安静，不硬找话说。
* 断网、超时不改人格、不编造已完成。
* 重启不重发同一封信。
* 不串台：初期只做 soul 级隔离，像 ViGil 一样——每个角色的信是她自己的，换聊天她记得自己写过。映射表、分支 hash 以后再说。

## 12. 已拍板

* 2026-09-06：端口 9003、衣橱 v1 纯文字、W2 和真搜索一起交、文档先行。
* 2026-09-07：不调 ViGil 口、全部新建；per soul 自扫、工具自选；删新鲜度、删相似度、删成本记账；SearXNG 换 9004；预算放到 20 次工具 / 100k tokens / 10 分钟
